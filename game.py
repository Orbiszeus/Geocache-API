import repository

class Game():
     
     def create_game_server(): #for admins only
     
          return 0
     
     def play_game(): #for normal users
          #this is the init function for the games 
          #when the game starts the game screen with map and it's caches
          #will be shown  
          #return the list of games current on mongo 
          return 0
     
     def leave_game():
          
          return 0 
     
     def reset_game(): #for admins only 
          
          return 0
     
     
     def check_if_cache_image():
          #first it is going to look at the location(lat/long)
          #then it is going to look at the image
          #this returns true if the cache image matches.
          return 0 